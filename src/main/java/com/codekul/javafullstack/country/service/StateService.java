package com.codekul.javafullstack.country.service;

import com.codekul.javafullstack.country.model.State;

import java.util.Map;

public interface StateService {
    String saveState(State state);

   // Map<String, Object> getStateByPincode(String pincode);

}
